# Nacos Gateway Configuration

server:
  port: 9848

# Routes configuration
routes:
  - host: "*.nacos.io"
    path: "/**"
    backend: example-service
    # qpsLimit: 1000        # Optional: override global QPS limit
    # maxConnections: 500   # Optional: override global max connections

# Backends configuration
backends:
  - name: example-service
    loadBalance: round-robin
    probe:
      path: /health
      periodSeconds: 10
      timeoutSeconds: 1
      successThreshold: 1
      failureThreshold: 3
    endpoints:
      - host: localhost
        port: 8080
        priority: 1

# Rate limiting configuration
rateLimit:
  globalQpsLimit: 10000
  globalMaxConnections: 5000
  defaultQpsLimit: 1000
  defaultMaxConnections: 500

# Timeout configuration
timeout:
  connectTimeoutSeconds: 10
  requestTimeoutSeconds: 30
  idleTimeoutSeconds: 60

# Logging configuration
logging:
  level: INFO
  verbose: true
  format: json           # text or json

# Management endpoint configuration
management:
  health:
    enabled: true
    path: /health
